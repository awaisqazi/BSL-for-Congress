---
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<section class="meet-hero" id="meet-hero" style={`background-image: url('${base}/images/meet-hero-placeholder.png')`}>
  <div class="meet-hero-overlay"></div>
  <div class="meet-hero-noise"></div>
  
  <div class="meet-hero-content container">
    <span class="meet-hero-badge animate-in">INDEPENDENT DEMOCRATIC SOCIALIST • CHICAGO DSA • IL-04</span>
    
    <h1 class="meet-hero-headline animate-in" data-i18n="meet.hero_headline">
      An Organizer, Not a Politician.
    </h1>
    
    <p class="meet-hero-sub animate-in" data-i18n="meet.hero_sub">
      From public school teacher to unapologetic democratic socialist Alderperson, Byron Sigcho-Lopez has spent his life fighting alongside the working class.
    </p>
  </div>
</section>

<style>
  .meet-hero {
    position: relative;
    min-height: 70vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
    overflow: hidden;
    padding-top: var(--nav-height);
  }

  @media (max-width: 768px) {
    .meet-hero {
      background-attachment: scroll;
      min-height: 60vh;
    }
  }

  .meet-hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      160deg,
      rgba(5, 173, 237, 0.9) 0%,
      rgba(3, 109, 153, 0.85) 40%,
      rgba(26, 26, 26, 0.8) 100%
    );
    z-index: 1;
  }

  .meet-hero-noise {
    position: absolute;
    inset: 0;
    z-index: 2;
    opacity: 0.04;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
    background-repeat: repeat;
    pointer-events: none;
  }

  .meet-hero-content {
    position: relative;
    z-index: 3;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-lg);
    max-width: 900px;
    padding: var(--space-4xl) var(--space-lg);
  }

  .meet-hero-badge {
    font-family: var(--font-display);
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.2em;
    color: var(--cream);
    background: rgba(230, 136, 28, 0.3);
    border: 1px solid rgba(230, 136, 28, 0.5);
    padding: 6px 16px;
    border-radius: 100px;
    backdrop-filter: blur(4px);
  }

  .meet-hero-headline {
    font-family: var(--font-display);
    font-size: var(--text-5xl);
    font-weight: 900;
    line-height: 1.05;
    color: var(--cream);
    text-transform: uppercase;
    letter-spacing: -0.02em;
    text-shadow: 0 4px 30px rgba(0,0,0,0.3);
  }

  .meet-hero-sub {
    font-family: var(--font-body);
    font-size: var(--text-xl);
    font-weight: 400;
    line-height: 1.6;
    color: #ffffff;
    max-width: 720px;
  }

  @media (max-width: 768px) {
    .meet-hero-headline {
      font-size: var(--text-4xl);
    }
    .meet-hero-content {
      padding: var(--space-3xl) var(--space-md);
    }
  }
</style>
